import type { Note } from "@/types";

/**
 * Notes route is reserved. Do not fill this with invented posts.
 * When Tosin has a note worth keeping, append a `Note` here and
 * set `draft: false`.
 */
export const notes: Note[] = [];
